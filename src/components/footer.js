import React from "react";
import Nav from "./nav";

const Footer = (props) => {
  return (
    <section className="footer">
      <Nav showPoster={false} />
    </section>
  );
};

export default Footer;
